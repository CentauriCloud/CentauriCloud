package org.centauri.cloud.cloud.network.packets;

import io.netty.buffer.ByteBuf;

public class PacketCloseConnection implements Packet {
	
	@Override
	public void encode(ByteBuf buf) {
	
	}

	@Override
	public void decode(ByteBuf buf) {
	
	}

}