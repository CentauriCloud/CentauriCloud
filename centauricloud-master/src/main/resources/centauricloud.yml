master:
    host: '127.0.0.1'
    port: 8012
    
template:
    startCommand: 'java -jar server.jar --port %port%'
    minServersFree: 1
    maxPlayers: 16

dependencies:
    - eula.txt:eula.txt
    - spigot.jar:server.jar